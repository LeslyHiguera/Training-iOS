import Foundation

var arregloEjemplo = [3,112,4,3,4,5,6,7,8,9]
//Operador de rango doble lado / two-sided range operator

var SuSetArreglo = arregloEjemplo [0..<3]

//Operador de rango unico lado / one-sided range operator

var SubSetArreglo2 = arregloEjemplo [4...]


var arregloCerrado = [1...5]

Array(arregloCerrado)

Array (arregloCerrado[0])

var SubArreglo = Array (arregloCerrado [0])

SubArreglo [3]
